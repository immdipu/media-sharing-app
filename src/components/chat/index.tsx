import React from "react";

const SingleMessage = () => {
  return <div>message</div>;
};

const index = () => {
  return (
    <div>
      <SingleMessage />
    </div>
  );
};

export default index;

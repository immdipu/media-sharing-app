import React from "react";
import SingleMessage from "@/components/chat/SingleMessage";

const index = () => {
  return (
    <div className="">
      <SingleMessage />
      <SingleMessage />
      <SingleMessage />
      <SingleMessage />
      <SingleMessage />

      <SingleMessage />
    </div>
  );
};

export default index;

import React from "react";
import Authentication from "@/components/Authentication/Authentication";

const page = () => {
  return (
    <div>
      <Authentication />
    </div>
  );
};

export default page;
